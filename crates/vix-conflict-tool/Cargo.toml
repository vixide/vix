[package]
name = "vix-conflict-tool"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Parse Git merge-conflict markers, resolve the conflict under the cursor, and list every conflict in a buffer for the Conflict overlay"

[dependencies]
vix-list-state = { path = "../vix-list-state", version = "1.6.0" }

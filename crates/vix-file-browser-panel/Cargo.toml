[package]
name = "vix-file-browser-panel"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "File browser panel (File → Open): recursive listing with fuzzy search, sorting, and filtering."

[dependencies]
walkdir = { workspace = true }
vix-palette = { path = "../vix-palette", version = "1.3.0" }

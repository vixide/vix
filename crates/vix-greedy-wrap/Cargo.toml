[package]
name = "vix-greedy-wrap"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Greedy word-wrap of a single paragraph to a column width, hard-breaking any word longer than the width so no line ever overflows."

[dependencies]

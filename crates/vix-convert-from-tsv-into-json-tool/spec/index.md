# Convert from TSV into JSON

Convert: TSV text into a JSON array of objects. Module `convert_from_tsv_into_json_tool`.

- menu "Tools"
  - submenu "Convert"
    - submenu "TSV"
      - menuitem "JSON"

Convert the selected text (or if no selected text then the entire buffer) from TSV into JSON.

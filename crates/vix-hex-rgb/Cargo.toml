[package]
name = "vix-hex-rgb"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "Lenient `#RRGGBB`/`RRGGBB` hex-string to `(r, g, b)` byte-triple parsing, treating any missing or invalid component as 0."

[dependencies]

[package]
name = "vix-lsp"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "LSP client: process management and document sync over `vix_lsp_core`."

[dependencies]
serde_json = { workspace = true }
vix-lsp-core = { path = "../vix-lsp-core", version = "1.3.0" }
vix-settings = { path = "../vix-settings", version = "1.3.0" }

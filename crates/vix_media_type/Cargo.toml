[package]
name = "vix_media_type"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
license.workspace = true
repository.workspace = true
description = "The IANA-style media-type (MIME) table and the picker's filter + selection."

[dependencies]

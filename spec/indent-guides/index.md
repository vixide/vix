# Indent Guides

Rendered decoration (no toggle action).

Draw a faint vertical bar at each indent level of space-indented lines, making block structure easy to follow.

`editor_core::render::draw_indent_guides`. See `spec/editor/index.md`.

See `spec/index/index.md` for the project overview and `spec/actions/index.md` for the full action catalog.

# Dictionaries

Dictionaries use a directory `dictionaries` with hunspell files `.dic` and `.aff` per locale.

Use the standard wooorm `dictionaries/` set (92 locales, hunspell index.aff + index.dic)

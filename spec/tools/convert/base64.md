# Base64

Module base64_tool.

- menu "Tools"
  - submenu "Convert"
    - submenu "Base64"
      - menuitem "Encode"
      - menuitem "Decode"

Convert the selected text (or if no selected text then the entire buffer) via encode/decode base64.
